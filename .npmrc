# npm 10's Arborist crashes building the ideal tree for this workspace shape
# ("Cannot read properties of null (reading 'edgesOut')" in #loadPeerSet).
# All peer deps are also direct devDeps, so legacy resolution loses nothing.
legacy-peer-deps=true
